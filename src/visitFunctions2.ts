/*import { notEqual, strictEqual } from "assert";
import Store from "./Store"

module.exports = function (babel) {
  const fs = require("fs");
  var handler; 
  var store =  new Store(); 
  var t = babel.types;
  let s = "";

  var output = [];
  return {
    visitor: {

      ClassMethod(path) {
        Store.p2.push(path.node.body)

      }
    
}
  }
}

*/



